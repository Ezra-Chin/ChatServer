<UserControl x:Class="PollingClient.src.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

	<Grid Background="White">

		<Border Width="400"
                Padding="40"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

			<StackPanel>

				<TextBlock Text="Chat"
                           FontSize="36"
                           FontWeight="Bold"
                           Foreground="#222222"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>

				<TextBlock Text="Sign in to continue"
                           FontSize="15"
                           Foreground="#777777"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

				<TextBlock Text="User ID"
                           FontWeight="SemiBold"
                           Foreground="#333333"
                           Margin="0,0,0,6"/>

				<TextBox x:Name="UserIdTextBox"
                         Height="40"
                         FontSize="15"
                         Padding="10"
                         Margin="0,0,0,20"/>

				<Button Content="Sign In"
                        Height="42"
                        FontSize="15"
                        Click="SignIn_Click"/>

			</StackPanel>

		</Border>

	</Grid>

</UserControl>